.productList
  = link_to item_path(item.id) do
    %figure.productList--img
    .productList--body
      %h3.name
        = item.images.first.image
        = item.name
      .details
        %ul
          %li
          = item.price
          円
          %li
            = fa_icon "heart", class: "likeIcon", text: item.text
        %p (税込)
